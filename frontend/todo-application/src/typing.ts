export type Task = {
    taskId: string,
    taskTitle: string,
    taskDescription: string,
    taskGroup: string,
    priority: string,
    duration: number,
    dueDate: Date,
    status: boolean
}
